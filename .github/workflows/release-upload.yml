name: Upload Release Assets

on:
  push:
    branches:
      - release-v*.*.*

permissions:
  contents: write

jobs:
  upload:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout release assets branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref_name }}

      - name: Reassemble installer
        run: |
          cat .release-chunks/exe.part* release-parts/exe.part* > Viberadio-1.1.1-Setup.exe
          cp dist/Viberadio-1.1.1-Setup.exe.blockmap .
          cp dist/Viberadio-1.1.1-SHA256SUMS.txt .
          sha256sum Viberadio-1.1.1-Setup.exe

      - name: Upload to Release
        run: |
          gh release upload v1.1.1 \
            Viberadio-1.1.1-Setup.exe \
            Viberadio-1.1.1-Setup.exe.blockmap \
            Viberadio-1.1.1-SHA256SUMS.txt \
            --repo x123dd/Viberadio \
            --clobber
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
